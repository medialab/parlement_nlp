# Sentence BERT (des)agreement

training script to fine-tune SentenceBERT model according to 1) anchor/agreement/desagreement triplets of speeches 2) agreement/desagreement pairs of speech from the french parlement.

Run `make datasets` to generate `duplet.csv` and `triplet.csv`.

Run `make train` to train the models.
